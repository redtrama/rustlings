fn main() {
    // TODO: Change the line below to fix the compiler error.
    // Rust doesn't have default values for variables.
    // If you want to initialize a variable that you just declared, you have to specifically assign a value to it.
    let x: i32 = 10;

    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
